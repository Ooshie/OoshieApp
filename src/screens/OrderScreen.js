import React from 'react'
import {View, StyleSheet, Text, Button} from 'react-native'

const OrderScreen = (props) => {
    return <View>
        <Text style={{ fontSize: 48}}>Orders Screen</Text>
    </View>
}

const styles = StyleSheet.create({});

export default OrderScreen;