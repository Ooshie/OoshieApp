import React from 'react'
import {View, StyleSheet, Text} from 'react-native'

const CartScreen = () => {
    return <View>
        <Text style={{ fontSize: 48, alignSelf: 'center', margin: 50}}>Cart Screen</Text>
    </View>
}

const styles = StyleSheet.create({});

export default CartScreen;